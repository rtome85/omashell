# omashell

Custom Omarchy shell experiments built with Quickshell.

Run the current workspace-switcher increment side-by-side with Waybar:

```bash
quickshell --path /home/roberto/Work/projects/omashell
```
